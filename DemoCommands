   ls -clear
   sudo su - //Command to move to root access
   aws s3 ls // command to list all buckets created in S3
   aws configure // to configure AWS CLI
   aws s3 ls 
   ls -la // List all files and folders
   cd .aws // move to new folder
   ls -la
   cat credentials // print contents of file
   rm -rf credentials // delete file
   ls -la
   cd .. // move to previous folder
   cd..
   aws s3 ls
   history // show history of commands used

    aws s3 mb s3://wesleebucket21sep // create bucket on default region
    aws s3 ls
    aws s3 mb s3://wesleebucketmumbai --region ap-south-1 // create bucket on specific region





AWS CLI Commands
=================
aws configure
The Configuring the AWS Command Line Interface documentation page lists various places where configuration files are stored, such as:

Linux: ~/.aws/credentials
Windows: C:\Users\USERNAME \.aws\credentials

aws s3 ls

aws s3 mb s3://weslee-static-website/ --region us-east-1
